{
  "name": "app-json-server-test",
  "version": "1.0.0",
  "description": "Simple json-based database to deploy to Heroku, to provide json data ",
  "main": "server.js",
  "keywords": [
    "json-server,heroku, node, REST API"
  ],
  "scripts": {
    "start": "node server.js",
    "start:dev": "nodemon server.js"
  },
  "dependencies": {
    "json-server": "^0.16.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}